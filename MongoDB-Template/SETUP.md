// Package dependencies needed for MongoDB integration
// Add these to your package.json dependencies

{
  "dependencies": {
    "mongoose": "^7.3.1",
    "dotenv": "^16.3.1"
  }
}

// Install with: npm install mongoose dotenv
